{
  "name": "repo-chat",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "check": "prettier --check .",
    "lint": "bunx oxlint"
  },
  "workspaces": [
    "packages/*"
  ]
}
